Run the `train_all_prism.sh` script to compute the PRISM weights for all configurations.

Alternatively, you can download the pre-trained PRISM weights from [here](https://github.com/mardgui/SEM/releases/tag/v1.0) and extract the `.pth` files in this directory.
