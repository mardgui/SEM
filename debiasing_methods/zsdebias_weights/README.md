Run the `train_zsdebias.sh` script to compute the ZSDebias weights for all configurations.

Alternatively, you can download the pre-trained ZSDebias weights from [here](https://github.com/mardgui/SEM/releases/tag/v1.0) and extract the `.pth` files in this directory.
